// Currently set to 1 - possible implementation, if using different keyboards then could allow
// for width and height to be set by user. For now will set to default values for macbook.
// will be set in pixels.

public enum {
    final int PICTURE_WIDTH = 0001;
    final int PICTURE_HEIGHT = 0001;
        }